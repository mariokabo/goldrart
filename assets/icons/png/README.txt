Drop PNG icons here (place actual PNG files and keep exact filenames below). Recommended sizes:
- Social icons (footer & header): 36x36 PNG (transparent background)
  filenames: facebook.png, instagram.png, whatsapp.png, telegram.png, tiktok.png
- Floating icons / badges: 64x64 PNG (decor): santa-hat.png, christmas-tree.png
Notes:
- If you add the PNG files with the exact names above, the site will show them automatically.
- You can replace the SVG versions if you prefer — keep both if you want fallback.
